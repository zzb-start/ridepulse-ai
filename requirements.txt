# RidePulse AI — Python 依赖
# Python 3.10+

# Web 体验界面
streamlit>=1.28

# 数据处理
pandas>=2.0
numpy>=1.24

# 数据模型与校验
pydantic>=2.0

# 配置
python-dotenv>=1.0

# HTTP 客户端
httpx>=0.25

# LLM 调用（OpenAI 兼容接口）
openai>=1.0

# 机器学习 / 聚类
scikit-learn>=1.3
# 本地多语言嵌入(EMBEDDING_MODE=local,CI 周更流水线使用)
sentence-transformers>=2.2

# 测试
pytest>=7.0
pytest-cov>=4.0

# JSON Schema 校验
jsonschema>=4.0
